import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  pokebolaBg: {
    position: 'absolute',
    width: 300,
    height: 300,
    top: -100,
    right: -100,
    opacity: 0.3,
  },
});
